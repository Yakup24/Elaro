<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />


    <application
    android:name=".MyApplication"
    android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:usesCleartextTraffic="true"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ETicaretSitesiSon"
        android:extractNativeLibs="true"
        tools:targetApi="31"
        tools:ignore="ExtraText,MissingClass">



       >


        <activity android:name=".activities.MainActivity" android:exported="true" />
        <activity android:name=".activities.ProductListActivity" android:exported="true" />
        <activity android:name=".activities.LoginActivity" android:exported="true" android:label="Giriş Yap" />
        <activity android:name=".activities.AccountActivity" android:exported="true" android:label="Hesabım" />
        <activity android:name=".activities.ProductDetailActivity" android:exported="true" android:label="Ürün Detayı" />
        <activity android:name=".activities.CartActivity" android:exported="true" android:label="Sepetim" />
        <activity android:name=".activities.OrderHistoryActivity" android:exported="true" android:label="Sipariş Geçmişi" />
        <activity android:name=".activities.AddressActivity" android:exported="true" android:label="Adreslerim" />
        <activity android:name=".activities.FavoritesActivity" android:exported="true" android:label="Favorilerim" />
        <activity android:name=".activities.RegisterActivity" android:exported="true" android:label="Kayıt Ol"
            tools:ignore="ExtraText" />

        <activity
            android:name=".activities.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.ETicaretSitesiSon">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>


    </application>

</manifest>
